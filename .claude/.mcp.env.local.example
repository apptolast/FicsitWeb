# Copia este archivo a .claude/.mcp.env.local y rellena tus claves reales.
# NO versiones .mcp.env.local — está en .gitignore.
#
# Google Stitch API key. Obtén/rota en Google Cloud Console.
# La que compartiste en el chat inicial quedó expuesta en el historial de la
# conversación — rota esa key antes de usarla aquí.
STITCH_API_KEY=
